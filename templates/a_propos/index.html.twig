{% extends 'base.html.twig' %}
{% block body %}
    <style>
        body{
            background-color: #25274d;
        }
        .contact{
            padding: 4%;
            height: 400px;
        }
        .col-md-3{
            background: #ff9b00;
            padding: 4%;
            border-top-left-radius: 0.5rem;
            border-bottom-left-radius: 0.5rem;
        }
        .contact-info{
            margin-top:10%;
        }
        .contact-info img{
            margin-bottom: 15%;
        }
        .contact-info h2{
            margin-bottom: 10%;
        }
        .col-md-9{
            background: #fff;
            padding: 3%;
            border-top-right-radius: 0.5rem;
            border-bottom-right-radius: 0.5rem;
        }
        .contact-form label{
            font-weight:600;
        }
        .contact-form button{
            background: #25274d;
            color: #fff;
            font-weight: 600;
            width: 25%;
        }
        .contact-form button:focus{
            box-shadow:none;
        }
    </style>
{{ form_start(form) }}

    <div class="container contact">
        <div class = "row">
            <div class="col-md-2">
                <div class="contact-info">
                    <img src="https://image.ibb.co/kUASdV/contact-image.png" alt="image"/>
                    <h3>Contact Us</h3>
                    <h4>We would love to hear from you !</h4>
                    {{ form_widget(form) }}
                </div>

            <div class="form-group">
                <input type="submit" value="Send" class="btn btn-primary py-3 px-5">
            </div>
            {{ form_end(form) }}

    {#   <div class="col-md-9">
             <div class="form-group">
                {{ form_start(form) }}
                {{ form_row(apropos.nom, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(apropos.email, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(apropos.message, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                <input type="submit" value="Send" class="btn btn-primary py-3 px-5">
            </div>

          </div>#}

{% endblock %}
